# 2026-05-31

## Moody Pro ZIT and Frontend Workflow Validation

- Added Moody Pro Mix `ZIT V10 R1 DPO` as the first committed Z-Image Turbo
  NSFW checkpoint workflow split: control commits `4fd7e49` and `1ea43bd`,
  model archive commit `84b8e02`.
- The initial staged workflow had corrupt GUI link data that API-equivalent
  testing and broad audits missed. The repaired workflow now passes the actual
  ComfyUI frontend path via Dockerized Playwright: GUI load,
  `app.graphToPrompt()`, `/prompt`, and `/history` success.
- Durable process change: `scripts/gui_workflow_smoke.mjs` and updated
  `AGENTS.md`, `models/ADDING_MODEL_OR_LORA.md`, and `workflows/README.md`
  make frontend smoke testing with `--submit --wait` required for new or edited
  canonical GUI workflow JSON. Hand-written or separately derived API prompts
  are not a substitute.

## Mystic XXX ZIT V7 LoRA Workflow

- Added the Mystic XXX ZIT V7 local reference workflow in control commit
  `2e4c93f`; no model-archive commit was needed because the existing Mystic
  sidecar/source PNG already covered the LoRA and reference artifact.
- Important lesson: for Civitai workflow-bearing images, compare website
  `meta.prompt` with embedded Comfy prompt/workflow nodes. This source used an
  external author-machine text-file node plus an appended text concatenate node,
  while the GUI widget text was stale and misleading.
- The smoke script was hardened in the same commit after a frontend startup race
  could otherwise submit ComfyUI's default graph instead of the loaded workflow.

## Dark Beast DBZiT9 RClaw Workflow

- Added Dark Beast `DBZiT9 DIM RClaw` as a Z-Image Turbo NSFW checkpoint split:
  control commit `98af9c2`, model archive commit `b48f605`.
- Reference image `124809807` embeds a workflow whose prompt path has no active
  LoRAs, but the GUI graph still contains disabled LoRA nodes. The broad audit
  caught the initial doc ambiguity; final docs explicitly distinguish active
  prompt-path dependencies from disabled/inactive GUI leftovers.
- Required frontend smoke passed via the actual ComfyUI frontend path and
  produced both preview and refined outputs.

## Photorealistic ZiT 8.2 checkpoint workflow

- Added Z-Image Turbo NSFW Photorealistic ZiT `8.2` BF16 as a local checkpoint
  split: control commits `a316a7a` and `2ea3f94`, model archive commit
  `395d7c4`.
- The accepted reference is Civitai image `131713347`. The workflow initially
  passed the required frontend smoke path, then was patched to use the source
  `ComfyUI-mnemic-nodes` resolution selector exactly; Marcin explicitly asked
  not to rerun validation after that patch.
- Reproduction note: nearby image `131713400` is not a clean exact-reference
  source because its PNG lacks a normal `parameters` chunk and still depends on
  wildcard/string prompt nodes. Its active sampler seed appears to come from the
  rgthree seed node, not the visible KSampler widget seed, so copied website
  prompt/seed pairs may drift even when the model stack matches.

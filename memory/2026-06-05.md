# 2026-06-05

## HunyuanImage 3 Instruct Workflow Widget Drift

- Fixed GUI widget serialization drift in the HunyuanImage 3 Instruct-Distil
  workflows with control commit `16ca60e`: both plain and multi-fusion workflows
  needed the modern ComfyUI seed-control widget value after `seed`; multi-fusion
  also had one stale extra low-VRAM value from the earlier preset consolidation.
- The Hunyuan low-VRAM custom-node patch itself was not the cause. The failure
  mode was positional GUI `widgets_values` drift; API prompt graphs that address
  inputs by name would not have had this specific problem.
- For future GUI workflow edits, validate widget-value alignment through the
  ComfyUI frontend conversion path before treating a canonical workflow as
  ready.

## Workflow Matrix Runner And SNOFS Evaluation

- Workflow driving was consolidated in control commits `8b93efc` and `18e6920`:
  the MJS helper only performs real ComfyUI frontend conversion, while Python
  owns prompt submission/history polling; the queue runner prints progress and a
  total-time summary, and `run_test_matrics.sh` sweeps prompts 6-8.
- Prompt/workflow metadata work now includes resolution front matter, SNOFS
  source prompt-term notes, and the updated `IDEAS.md` entry for future
  style-keyword LoRA activation (`47a01ef`).
- After sweeps of prompts 5-8 at batch 4, most workflows failed two-person body
  contact either through anatomy/body-fusion failures or pose non-adherence.
  SNOFS Qwen 2512 was the only clear current anchor for mostly valid contact
  poses; its plastic/washed character rendering should be treated as a workflow
  tuning/detailer problem before disqualifying it.
- Next likely experiment should be one controlled reference/pose-conditioning
  branch on SNOFS Qwen 2512 only, not a 25-workflow retrofit. Broader style or
  fantasy-character coverage should wait until the pose-control tradeoff is
  understood.

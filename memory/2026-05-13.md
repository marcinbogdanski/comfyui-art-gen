# 2026-05-13

## Memory and Process Notes

- Added memory-file guidance to `AGENTS.md`: read recent
  `memory/YYYY-MM-DD.md` files, use Marcin's logical day rather than strict
  midnight when naming entries, append useful worklog/handoff notes, and keep
  memory for setup lessons and process decisions.
- Documented that the GitHub CLI is available and pre-authenticated for agent
  access, with the explicit constraint not to force-push or delete the main
  branch.

## Video Model Research Handoff

- Did a quick current scan for local NSFW short video generation. The practical
  default community direction looked like Wan, especially Wan 2.1 / 2.2 I2V
  14B, usually through ComfyUI.
- For this box and use case, the recommended starting path was:
  `Wan 2.1 I2V 14B 480p FP8` plus `ComfyUI-WanVideoWrapper`, with
  `ComfyUI-VideoHelperSuite` and possibly KJNodes depending on the workflow.
- The recommended workflow shape was image-first rather than pure text-to-video:
  generate/control a strong first frame with the existing image stack, then use
  Wan I2V to animate a few seconds of simple repetitive motion, optionally
  adding a purpose-trained Wan motion/action LoRA.
- Wan 2.1 was called out as a safer first target than chasing every newer
  variant because it has a mature local workflow and LoRA ecosystem.

## Wan 2.2 Kijai Setup

- User chose to try Wan 2.2 first and back off only if it causes problems.
- Installed persistent custom nodes under `/mnt/data/comfyui/custom_nodes`:
  `ComfyUI-WanVideoWrapper` d18cdb1, `ComfyUI-VideoHelperSuite` a6879b8,
  and `ComfyUI-KJNodes` fca78c9.
- Updated the Docker image dependencies for Kijai WanVideoWrapper,
  VideoHelperSuite, and KJNodes; rebuilt `local/comfyui:cu130` successfully.
- Added `COMFYUI_DETACH=1` support to `run_comfyui.sh`; default foreground
  `docker run --rm -it` behavior is unchanged.
- Downloaded upstream Kijai Wan 2.2 workflows unchanged to
  `/mnt/data/comfyui/user/default/workflows/wan22-upstream` and created
  editable `.work.json` copies under
  `/mnt/data/comfyui/user/default/workflows/wan22-work`.
- Work copies changed only the `LoadImage` input to
  `BigLoveKlein1_00070_.png` and the `WanVideoModelLoader` attention mode from
  `sageattn` to `sdpa`, because `sageattention` is optional and not installed.
  After API validation, normalized model path separators from Windows `\` to
  Linux/API-friendly `/` in the work copies.
- Created an API-format executable prompt at
  `/mnt/data/comfyui/user/default/workflows/wan22-work/wanvideo_2_2_I2V_A14B_example_WIP.api.json`.
  Validation reached expected model-file errors only; after path normalization,
  the remaining validation blocker was the not-yet-downloaded LOW A14B model.
- Started ComfyUI detached as container `comfyui`; startup imported
  WanVideoWrapper, VideoHelperSuite, and KJNodes successfully. Current warnings:
  optional `sageattention` missing and FantasyPortrait `onnx` missing. The
  basic Wan 2.2 I2V workflows only show missing `Note` annotation nodes; the
  controlnet workflows also need MiDaS preprocessor nodes if used later.
- Added model/workflow download notes in `models/wan2.2-kijai.md`.

## SimpleMath+ / ComfyUI Essentials

- Installed `SimpleMath+` by cloning `cubiq/ComfyUI_essentials` into
  `/mnt/data/comfyui/custom_nodes/ComfyUI_essentials` at commit `9d9f4be`.
- Added the node package dependencies to the ComfyUI Docker image:
  `colour-science`, `numba`, `pixeloe`, `rembg`, and
  `transparent-background`.
- Rebuilt `local/comfyui:cu130`, restarted detached container `comfyui`, and
  verified `SimpleMath+` via `http://127.0.0.1:8188/object_info`.

## Wan 2.2 No-Compile API Test

- Created no-compile copies of the Wan 2.2 I2V workflow:
  `/mnt/data/comfyui/user/default/workflows/wan22-work/wanvideo_2_2_I2V_A14B_example_WIP.no_compile.work.json`
  and
  `/mnt/data/comfyui/user/default/workflows/wan22-work/wanvideo_2_2_I2V_A14B_example_WIP.no_compile.api.json`.
- Removed only the `WanVideoTorchCompileSettings` links from both
  `WanVideoModelLoader` `compile_args` inputs; left the original work/API files
  unchanged.
- Submitted API prompt `ec2ab82b-18c1-4b76-9847-bf50cbb70a12`; it completed
  successfully with 81 frames at 704x704. Peak sampler reserved memory was
  about 13.1 GB on GPU 0.
- VideoHelperSuite had `save_output=false`, so the initial MP4 was written to
  container temp. Copied it to persistent host output:
  `/mnt/data/comfyui/output/WanVideo2_2_I2V_00001.mp4`.
- Updated the no-compile GUI/API workflow copies to set
  `VHS_VideoCombine.save_output=true` so future videos are saved under
  `/mnt/data/comfyui/output`.
- User generated a second successful video at 81 frames, 480x832, 16 fps;
  copied it from container temp to
  `/mnt/data/comfyui/output/WanVideo2_2_I2V_00002.mp4`.

## Runtime State

- Restarted llama.cpp server as detached container `llama` on physical GPU 1,
  port 8081, using
  `HauhauCS/Qwen3.5-35B-A3B-Uncensored-HauhauCS-Aggressive:Q4_K_M`.
- ComfyUI remains detached as container `comfyui` on physical GPU 0, port 8188.

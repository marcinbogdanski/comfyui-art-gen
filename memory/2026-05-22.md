# 2026-05-22

## Model Archive Cleanup

- Split model inventory/rules/checking out of `art-gen-ctrl` and into the
  dedicated `/mnt/data/comfyui/models` repo, leaving this repo responsible for
  Docker/runtime operations and runnable workflow references.
- Clarified the external model archive policy: sidecar `.md` plus raw `.html`
  preserve source context; real weights are explicitly git-ignored; workflow
  artifacts, references, LoRA applicability, and required model files are
  recorded in the sidecar JSON.
- Cleaned up the current model/LoRA sidecar index enough that remaining
  untracked or failing files are intentionally deferred, mostly CognitiveRiver
  plus related controlnet/upscaler work for a new thread.
- Consolidated Qwen GGUF usage around the local 35B file and updated the repo
  workflow to select the local model path after ComfyUI exposed the correct
  model entry.

## Workflow Repository Cleanup

- Removed checked-in ComfyUI API prompt graphs from `workflows/`; the repo now
  keeps canonical GUI-loadable workflow JSON while live `.api.json` and
  `.work.json` files stay outside git.
- Normalized tracked workflow JSON with `jq .` so future diffs are readable and
  deterministic enough for review.
- Deleted redundant per-workflow notes where the useful Qwen workflow context
  fit better in `models/qwen3.5-35b-a3b.md`; durable Qwen model/runtime usage
  lives in the `/mnt/data/comfyui/models` sidecar.
- Created local ignored `.work.json` copies for non-Hunyuan workflows so the
  user can edit working copies without tracking ComfyUI save churn.

# 2026-07-31

- Completed the Krea 2 rollout on `krea2-models-smoke-matrix`, based on
  `origin/main`. Control commits: `21a9fc5` (ComfyUI/Docker and inventory
  smoke), `2bc194b` (official RAW/Turbo), and `9506938` (community models,
  unlock, docs, and expanded matrix). Model-archive commit: `a3cc335`.
- ComfyUI 0.29.0 rebuilt and verified across four one-GPU containers with
  separate databases. Dependency smoke covered the prior supported inventory
  plus nine Krea paths. The original 25-workflow matrix passed, then the
  expanded 34-workflow frontend dry-run and full generation matrix passed
  34/34 with independently decoded, non-black outputs.
- Added official Krea 2 RAW/Turbo, all six requested Civitai assets, and
  TextFusion refusal reduction as the default standalone unlock. Source
  artifacts and exact inventory are recorded in `models/krea-2/krea2.md` and
  the model archive. Independent staged audit passed both repositories.
- No hardware fault, sudo need, push, or merge occurred. Next step is human
  review, then merge the matching feature branches when accepted.

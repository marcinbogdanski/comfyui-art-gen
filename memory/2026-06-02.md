# 2026-06-02

## Workflow matrix normalization and runner tooling

- Normalized the 25-workflow `workflows/test_matrix.txt` set around the current
  canonical naming pattern (`<family>_<variant>_<asset-type>_<asset-short-name>`),
  single-image output expectation, stable output prefixes, unpacked workflows,
  fixed seeds/randomness, and script-driven generation assumptions. Key control
  commits from this pass include `1e78c2d` through `8c3ddde`.
- Added and iterated the simple script workflow runner: `scripts/workflow_prompt.py`
  and `scripts/workflow_queue.py`. Current runner supports prompt file input,
  `-w/--workflow`, `--workers`, `--batch`, `--seed`, `--id`, output-skip for
  existing run IDs, memory `/free` before/after jobs, OOM trace suppression, and
  three total attempts. Important retry lesson: do **not** call `/free` between
  retry attempts, because ComfyUI's upstream-node cache can make the retry pass.
- Qwen/Jib Mix and some FLUX.2 jobs can still OOM on cold attempts under parallel
  matrix sweeps; this is expected operationally. The script now reports concise
  `CUDA OOM error` lines and retries. User reported later matrix sweeps worked.

## Workflow Metadata note contract

- Every matrix workflow now has exactly one plain ComfyUI `Note` titled
  `Metadata`, with JSON front matter followed by `---` and concise free-form
  source notes. The front matter includes `trigger_words` and `trigger_required`
  (`required`, `optional`, `no`, or `unknown`).
- `workflow_prompt.py` is the single source of truth for consuming this metadata:
  it validates the note shape with simple assertions and prepends required
  trigger words to scripted prompts. As of this session, the only required
  trigger in the matrix is Stable Yogi's `99bsy99`.
- Docs were updated in `AGENTS.md`, `workflows/README.md`, and
  `models/ADDING_MODEL_OR_LORA.md`. The required no-generation preflight for
  the full matrix is now:
  `python3 scripts/workflow_queue.py prompts/prompt1.md --dry-run`.

## Prompts and future workflow-control ideas

- Added tracked prompt examples `prompts/prompt2.md` through `prompt4.md` and
  ignored `scripts/__pycache__/`.
- Captured future prompt/workflow-control directions in `IDEAS.md`: structured
  prompt files with positive/negative/keywords, negative prompt routing,
  keyword-to-trigger mapping, keyword-to-LoRA activation, and keyword-to-ControlNet
  or reference-branch activation. These are intentionally ideas, not specs.
- No model-archive commits were made during this workflow-control/tooling
  session; recent archive state remains the matrix/archive cleanup ending at
  `a8a3fd8`.

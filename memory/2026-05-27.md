# 2026-05-27

## Z-Image Turbo LoRA Experiments

- User experimented on May 26-27 with Z-Image Turbo plus ZIT LoRAs for
  explicit/creature scenes. Doggy-style composition was not automatic from pose
  words alone; it needed careful spatial wording such as describing hip contact.
- Mystic-XXX-ZIT-V7 was useful and should be kept available: it helped fix the
  recurring penis-stub artifact and worked at light strength. Z-Detail-Slider
  and zimage_lightslider were also useful. Civit_Art_V1T1 can be added lightly
  for a style touch.
- Position LoRAs tended to damage non-human/fantasy characters, especially
  zombies, by pulling them toward human forms. DOGGYSTYLE_ZIT and
  ZiTMythR3alisticF were deleted after testing; doggystyle_facing_camera remains
  downloaded but should be treated cautiously for fantasy/creature prompts.
- The full fp16 Moody Pro Mix ZIT V10 R1 DPO model was downloaded and verified,
  but user testing found dedicated porn models could destroy character identity,
  so do not assume it is a better default for these scenes.
- Demon/ogre/zombie dungeon scenes can work, but are fragile with LoRAs. Put
  creature identity and pose stability before heavy style/position LoRA stacks.
  Empty-room continuity worked when prompts included identifying room items.
- Simple one-person standing/sitting scenes were comparatively reliable. Avoid
  contradictory camera/composition instructions such as close-up plus full-body
  framing in the same prompt.

## Runtime Note

- ComfyUI was OOM-killed after heavy HunyuanImage3 block-swap work and restarted
  successfully. If the web UI disconnects, check `docker inspect comfyui` for
  `OOMKilled=true` before assuming a frontend-only issue.

## Ogre Elf Z-Image Workflow Setup

- Cleaned `workflows/ogre_elf.json` down to the selected Z-Image text-to-image
  path and removed post-processing grain/sharpen nodes so the saved image comes
  directly from the generation subgraph.
- Installed the generation-affecting custom nodes locally:
  `ComfyUI-Detail-Daemon` and `RES4LYF`, pinned to the workflow-recorded
  commits. Added their dependency/setup coverage to the repo Docker/Vast setup.
- Downloaded the GGUF files required by the cleaned workflow into the external
  model archive with source sidecars. The active stack is now Unsloth-sourced:
  `Qwen3-4B-UD-Q6_K_XL.gguf`, `z-image-Q8_0.gguf`, and
  `z_image_turbo-Q8_0.gguf` whose hash matches Unsloth's Q8 turbo file despite
  the local workflow-compatible underscore filename.
- Downloaded and verified the active Civitai LoRAs:
  `128 bean effect 3_000101000.safetensors` and
  `ZIB-Image-Fun-Lora-Distill-2603_UDCAI_ComfyUI.safetensors`. After replacing
  an incompatible non-Unsloth base GGUF, a reduced neutral API smoke test
  completed successfully and saved `ogre_elf_smoke_00001_.png`.

## Z-Image Turbo ControlNet Experiment

- Early Z-Image Turbo ControlNet tests were inconclusive for fantasy/creature
  body-contact prompts. The ControlNet path can force pose/layout, but it also
  pulled creature identity toward humans and did not fix the recurring
  penis-stub artifact. Keep it available as an experiment, not a default.

# 2026-05-18

## Civitai API Key Check

- `CIVITAI_API_KEY` is available from `~/.bashrc`.
- Authenticated Civitai downloads work with:
  `Authorization: Bearer ${CIVITAI_API_KEY}`.
- Verified a fresh authenticated download of
  `cyberrealisticPony_v180Coreshift.safetensors` against the local file; size,
  MD5, SHA256, and byte comparison matched.
- Updated `models/cyberrealistic-pony.md` to document that the Civitai
  checkpoint now requires an API key.

## VastAI 4090 Test Instance

- Destroyed the first stopped/misconfigured Vast instance and recreated a
  clean RTX 4090 rental with explicit ComfyUI port mapping.
- The registered Vast SSH key was passphrase-protected and no local SSH agent
  was available in the Codex shell, so a temporary local key was created and
  attached to the instance. For future runs, either start an agent or create a
  short-lived task key intentionally.
- Added `scripts/setup_vast_comfyui.sh`, copied it to the instance, and ran it.
  The script installs ComfyUI `v0.20.1`, the same Python dependency set as the
  Dockerfile, and the known custom nodes pinned to observed commits.
- Started ComfyUI with:
  `/workspace/run_comfyui.sh --disable-api-nodes`
- Verified `/system_stats` reports ComfyUI `0.20.1`, PyTorch `2.9.1+cu130`,
  and a CUDA RTX 4090 with about 24 GB VRAM.
- Copied the Flux Schnell GUI workflow to:
  `/workspace/ComfyUI/user/default/workflows/flux-schnell/flux_schnell_fp8.json`
- Downloaded Flux Schnell FP8 checkpoint to:
  `/workspace/ComfyUI/models/checkpoints/flux1-schnell-fp8.safetensors`
- Confirmed ComfyUI sees `flux1-schnell-fp8.safetensors` in
  `CheckpointLoaderSimple`.
- Decided not to add model-download scripts yet; the model `.md` commands are
  clear enough, and scripts would be premature until repetition becomes painful.
- For Civitai downloads on rented instances, prefer resolving a short-lived
  signed URL locally with `CIVITAI_API_KEY`, then passing only that URL to the
  remote instance instead of storing the API key there.

# 2026-05-28

## Wan 2.2 Paired LoRA Wiring

- Added the missing paired Wan LoRA archive records externally and committed
  them in `/mnt/data/comfyui/models` as `3796661 Add Wan 2.2 paired LoRA
  references`: `Pronebone_low_noise`, `sfbehind_v2.1_high_noise`, and
  `sfbehind_v2.1_low_noise`.
- Confirmed `sfbehind` v2.1 declares `Wan Video 2.2 I2V-A14B`, matching the
  local Kijai Wan 2.2 I2V HIGH/LOW workflow family.
- Formatted and sanity-checked
  `workflows/wan22/wanvideo_2_2_I2V_A14B_example_WIP.me.json`. The HIGH branch
  chains `sfbehind_v2.1_high_noise -> Lightx2v -> WanVideoSetLoRAs`; the LOW
  branch chains `sfbehind_v2.1_low_noise -> Lightx2v -> WanVideoSetLoRAs`.
  Structural link checks passed, but no generation smoke test was run.

## Z-Image Turbo ControlNet Setup

- Downloaded the Alibaba-PAI Z-Image-Turbo Fun ControlNet Union lite 2602
  model patch externally under `/mnt/data/comfyui/models/model_patches`; its
  sidecar was later committed in the model archive as `f01f779 Add Z-Image
  Turbo ControlNet patch reference`.
- Local ControlNet wiring uses `LoadImage -> AIO_Preprocessor` with
  `DepthAnythingV2Preprocessor`, a `PreviewImage` for the depth map,
  `ModelPatchLoader`, and `ZImageFunControlnet` before the sampler model input.
- Official Comfy workflow uses ControlNet strength `1.0`. Alibaba-PAI documents
  a useful scale range around `0.65-1.00`; for local LoRA-heavy/body-contact
  tests, start around `0.65-0.8` and tune upward only if structure is too weak.

## Z-Image Base Ogre Elf Results

- First real `z_image_base_ogre_elf` tests with the Bean Effect and Z-Image Fun
  Distill LoRAs were very promising: strong NSFW handling, no or very low body
  horror, good creature retention, and good though imperfect body positioning.
- Compared with Z-Image Turbo, the base workflow appears to retain much of
  Turbo's creativity while giving slightly better positioning. It is worth more
  focused experiments before changing defaults.

## Modern Original Model Workflow Pass

- Implemented the modern original model setup plan and committed the control
  repo as `87871f9 Add modern original model workflows`. The useful canonical
  FLUX.2 baseline paths are now `flux-2/dev/original/image_flux2_text_to_image`
  for Dev, `flux-2/klein/original/image_flux2_text_to_image_9b` for Klein, and
  the existing BigLove Klein branch for NSFW Klein-family work. Older
  `flux2_example` and `image_flux2_fp8` are reference/image-guidance workflows,
  not the clean baseline paths.
- Committed matching model-archive records as
  `4af75fb Add modern original model references`, then added the remaining
  Z-Image Turbo LoRA sidecars as `17e904e Add Z-Image LoRA sidecars`.
  `model_summary_check.py` passed after documenting `Mystic-XXX-ZIT-V7` and
  `Z-Detail-Slider`.
- The official workflow LoRAs needed for GUI validation are downloaded and
  documented: FLUX.2 text-to-image `Flux_2-Turbo-LoRA_comfyui`, FLUX.2 FP8
  image-edit `Flux2TurboComfyv2`, Qwen 2512 Lightning, and Qwen Edit 2511
  Lightning. These LoRAs remain optional for baseline smokes, but ComfyUI
  validates filenames when loading official templates.
- Smoke coverage succeeded for the six original families: FLUX.2 Dev, FLUX.2
  Klein 9B, Qwen-Image 2512, Qwen-Image-Edit 2511, Z-Image Turbo, and Z-Image
  Base. FLUX.2 Dev official BF16 text-encoder path passed only as a small
  256px/1-step smoke on the 24 GB 8188 instance; a 512px run hit CUDA OOM.
- Z-Image Base official workflow is not using a Turbo/distill LoRA. It loads
  `z_image_bf16`, `qwen_3_4b`, and `ae`, with `KSampler` set to 25 steps,
  CFG 4, `res_multistep`, `simple`; that lower step count likely explains why
  it feels fast.
- Runtime state at session capture: four ComfyUI containers are running on
  ports 8188, 8189, 8190, and 8191. The llama.cpp container was stopped so
  GPU 3 could run `comfyui-gpu3` on port 8191. The 20 newest files in
  `/mnt/data/comfyui/output` had mtimes adjusted into grouped model order for
  easier modified-date sorting; contents were not changed.

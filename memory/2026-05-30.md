# 2026-05-30

## Jib Mix Qwen v6 base reproduction

- Completed the jib-mix base (no-LoRA) reference split: control `79187c4`,
  archive `a435198`. Recipe, node choices, and the `SeedVarianceEnhancer`
  dependency are in those commits and the workflow/model-doc notes.
- Open decision: the `SeedVarianceEnhancer` custom node lives only in the
  `/mnt/data/comfyui/custom_nodes` mount, untracked by Dockerfile/manifest, so a
  fresh host won't have it. Decide durable tracking before reusing this workflow.
## Jib Fix cleanup

- Dropped Jib Fix as a supported Jib Mix/Qwen path after review showed no clean
  reproducible v6+fix source and local tests showed negligible effect.
- Control commit `5e73d1a` moved the Jib Mix v6 workflow under the 2512 NSFW
  folder, removed Jib Fix from the Qwen LoRA stack and rebuild smoke test, and
  kept workflow notes minimal.
- Model archive commit `0e27ffe` removed the Jib Fix source record/artifacts and
  made `jibMixQwen_v60.png` (Civitai image `123554279`) the only archived Jib
  Mix v6 workflow artifact.
- `HANDOVER.md` is stale and can be dropped.

## SNOFS Qwen LoRA reference workflow

- Added the SNOFS Qwen reference split: control commit `198c820`, archive commit
  `918368b`. The accepted local workflow is
  `workflows/qwen-image/2512/nsfw/image_qwen2512_lora_snofs.json`; model/archive
  source records are in the paired commit.
- Process correction from the session is now committed in control `3d67cc5` and
  archive `3a758e1`: model-archive staged checks must use a fresh-context Codex
  sub-agent with live progress, and the Claude CLI review pattern is documented
  in `AGENTS.md`.
- Adjacent Qwen/Qwen4Play/MCNL/original-model work was intentionally left
  unstaged in both repos. Future agents should inspect status carefully and not
  assume those files belong to the SNOFS commits.

## MCNL original Qwen-Image workflow

- Added the MCNL original Qwen-Image safetensors split: control commit
  `20cba14`, model archive commit `b103800`. The runnable workflow is
  `workflows/qwen-image/base/nsfw/image_qwen_lora_mcnl.json`; source/archive
  artifacts are in the paired model archive commit.
- The workflow was renamed before commit to match the new canonical pattern
  documented in `AGENTS.md`: `<prefix-or-model-name>_<lora-or-model>_<lora-short-name>.json`.
- Review process lesson: when Claude is requested for independent review, follow
  the documented `AGENTS.md` command exactly, including
  `--dangerously-skip-permissions`, `--verbose`, and `stream-json`. A
  permission-checked Claude run missed the model archive git index; the corrected
  run plus a fresh Codex sub-agent both passed.
- Qwen4Play/2512 work later moved into its own review scope; remaining adjacent
  SNOFS/Qwen scratch files should still not be folded into MCNL/SNOFS follow-up
  work without a separate explicit scope.

## Qwen4Play 2512 review and process correction

- Added the accepted Qwen4Play 2512 workflow/archive split: control commit
  `48dc249`, model archive commit `1ed827d`. The workflow was tested on local
  node `8190` and accepted as close enough to the Civitai reference image.
- Fixed the model archive boundary miss from review: archive sidecars must stand
  alone and must not point at `art-gen-ctrl` workflow paths. The archive commit
  also added a deterministic check for control-repo references and cleaned an
  older CyberRealistic sidecar violation.
- Follow-up control commits captured adjacent context and process improvements:
  `3a09e7c` documents the SNOFS 2512 fallback workflow, and `50b5b3b` tightens
  the Claude review instructions so future independent audits read both repos'
  rules, discover the changed scope themselves, and check filenames, boundaries,
  sidecar references, staged files, and untracked files literally.

## RedCraft Z-Image Base checkpoint

- Added RedCraft ERNIE RedMIX `ZIB Distilled FUNxAGILE` as the first Z-Image
  Base NSFW/fantasy coverage checkpoint: control commit `e70b2e7`, model
  archive commit `7a29b5f`.
- The accepted reference is Civitai image `123529843`, a workflow-bearing
  flamingo scene chosen for broad non-human/concept coverage rather than human
  anatomy. The runnable control workflow is
  `workflows/z-image/base/nsfw/image_z_image_redcraft_zib_agile.json`; it keeps
  only the first-pass text-to-image path and passed a ComfyUI API smoke test.
- Archive validation passed with the staged checklist and a broad two-repo
  validator. The one important ambiguity is documented in the sidecar: the
  embedded source workflow names the UNet
  `RedZFUN-v6-ZIB-Distilled-AGILE-8steps-ComfyUI_00001_.safetensors`, mapped
  locally to Civitai's verified
  `redcraftERNIERedmix_zibDistilled.safetensors`.

## Moody Wild Z-Image Base checkpoint

- Added Moody Wild Mix (ZIB/ZID) `V4.0 BASE 40STEPS CFG4` full FP16 as the
  first Z-Image Base NSFW/human-anatomy checkpoint after RedCraft: control
  commit `e31cce6`, model archive commit `de3162b`.
- The accepted source is Civitai image `130509460`, a workflow-bearing PNG
  served from a `.jpeg` URL. The local workflow is
  `workflows/z-image/base/nsfw/image_z_image_moody_wild_v4_base.json` and keeps
  the core two-pass Z-Image Base path while omitting unsupported polish branches
  such as UltimateSDUpscale, detailers, skin contrast, and SeedVR2.
- The Civitai version exposes FP8 and FP16 files with the same displayed
  filename; the archive intentionally keeps the full FP16 as
  `diffusion_models/moodyWildMixZIBZID_v40BASE40STEPSCFG4_fp16_full.safetensors`
  and documents the mapping from the source workflow's local UNet name. API
  smoke test passed and both required archive and broad two-repo validators
  passed before commit.

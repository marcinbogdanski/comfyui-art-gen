# 2026-08-31

## Krea 2 rollout completion

- The Krea 2 rollout is complete and merged into control `main` at `b93bf99`.
  The final candidate content landed in control commit `421ed72` and model-archive
  commit `c2e12c1`; subsequent audit-policy commits are `9d1d3fd` and `2eccd15`.
- The supported set now has 15 canonical Krea 2 workflows with model/archive
  records, smoke coverage, and reference validation. Technical details belong in
  `models/krea-2/krea2.md`, the model archive, and git history rather than here.
- A saved-default regression batch completed 15/15 through the frontend/API path
  and was renamed to `ref02_*.png` under `/mnt/data/comfyui/output/`. The images
  are intended to be recorded in a separate reference-image repository.
- The temporary untracked `KREA2_NOTE.md` and `KREA2_NOTE_2.md` files were later
  deleted at Marcin's request. The session-level matrix baseline and
  proportional targeted-retesting guidance is already captured in `AGENTS.md`.

## PornMaster, Moody Desire, and Qwen4Play v2

- Added FLUX.2 Klein PornMaster V4 Turbo FP8 and Moody Desire Mix v3 FP8 with
  archived source records and reference-adapted workflows (`c9f821b` control,
  `b9e3ce8` archive).
- Added Qwen4Play 2512 v2 as a separate LoRA/workflow candidate (`4c06713`
  control, `b435701` archive). Its published v2 workflow reproduced the selected
  creator image closely; keep v1 until a direct comparison justifies deprecation.
- The Qwen creator image embeds an experimental Edit-LoRA filename rather than
  the published v2 file; that source mismatch is documented in the archive and
  workflow metadata.

## Interactive process boundary

- Added the authoritative **ITERATE**/**FINALIZE** phase gate in `AGENTS.md` and
  aligned `models/ADDING_MODEL_OR_LORA.md`: interactive work now stops unstaged
  after targeted validation, while commit requests reuse existing evidence and
  report missing required checks instead of launching them automatically.

## Z-Image Turbo refresh

- Added Moody Pro Mix Collector's Edition FP8, Stable Yogi 2603 FP8, and
  PornMaster Turbo V3.5 BF16 with archived source records and separate canonical
  reference workflows (`55ec729` control, `1272b12` archive). Existing Moody Pro
  V10 R1 and Stable Yogi 2602 remain available; nothing was deprecated.
- Marcin accepted all three targeted outputs: PornMaster reproduced its creator
  image effectively identically, Moody was close with background drift, and
  Stable Yogi had more substantial background drift but remained acceptable.
- The three workflows are in the main matrix; targeted frontend/API generation,
  the expanded 46/46 conversion baseline, archive audit, and cross-repo
  integration audit all passed. Detailed mappings and substitutions belong in
  the committed model/workflow records.

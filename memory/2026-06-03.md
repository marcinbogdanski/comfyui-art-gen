# 2026-06-03

## Multi-day process consolidation, 2026-05-30 through 2026-06-03

- From 2026-05-30 through 2026-06-03, the repo went through a concentrated
  model/workflow addition and process-hardening pass. Individual model and
  workflow outcomes are recorded in the dated memory entries and git history; do
  not duplicate those details here.
- Durable operating lesson from conversation reviews: Codex agents repeatedly
  overstepped during discussion/review turns. The current `AGENTS.md` action
  boundary is intentionally strong and duplicated: read-only research is allowed,
  but edits, file creation, staging, commits, downloads to durable locations, and
  cleanup require a direct explicit human instruction.
- `models/ADDING_MODEL_OR_LORA.md` now encodes the expected model/LoRA addition
  shape: research-only scope manifest first, implementation only after explicit
  execution instruction, separate archive/control scopes, separate staging, and
  fresh-context sub-agent audits for staged model-archive/two-repo review when
  applicable.
- Sub-agent wording is intentionally portable and not tied to Codex. Claude CLI
  notes were moved out of `AGENTS.md`; future agents should not use Claude CLI
  unless the human explicitly points them to that instruction file or asks for
  it.
- Workflow matrix validation should stay on the real runner path. The required
  no-generation matrix preflight is now:
  `python3 scripts/workflow_queue.py prompts/prompt1.md --dry-run --batch 1 --seed 1 --id dryrun_matrix`.
  `scripts/workflow_prompt.py` remains the single place that validates and
  consumes workflow Metadata trigger front matter during scripted prompt
  handling.
- `workflows/test_matrix.txt` is a curated regression set, not an automatic
  inventory. Do not add newly created workflows to it unless the human asks or
  the accepted scope explicitly includes matrix membership.
